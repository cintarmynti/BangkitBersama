import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const MakeHelp = () => {
    return (
        <View>
            <Text>Make Help Page.</Text>
        </View>
    )
}

export default MakeHelp

const styles = StyleSheet.create({})
